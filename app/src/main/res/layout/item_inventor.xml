<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="250dp"
    android:layout_height="340dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:layout_margin="12dp"
    android:id="@+id/itemInventor"
    >

    <LinearLayout
        android:id="@+id/itemBg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_marginTop="30dp"
        android:layout_marginLeft="30dp"
        android:layout_marginBottom="25dp"
        android:gravity="bottom"
        android:background="@drawable/bg_galileo1"
        >
        
        <TextView
            android:id="@+id/inventor_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nikola Tesla"
            android:textColor="@color/black"
            android:textSize="28sp"
            android:textStyle="bold"
            android:layout_marginStart="12dp"
            android:layout_marginBottom="4dp"
            />

        <TextView
            android:id="@+id/inventor_abut"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Daho fizik Nikola Tesla \n(1856–1943) -amerikalik \nfizik va ixtirochi"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="22dp"
            android:textColor="@color/black"
            android:maxLines="3"
            android:ellipsize="end"
            />
        <Space
            android:layout_width="match_parent"
            android:layout_height="24dp"/>
    </LinearLayout>

<de.hdodenhof.circleimageview.CircleImageView
    android:id="@+id/inventor_img"
    android:layout_width="150dp"
    android:layout_height="150dp"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    android:src="@drawable/galileo_img"
    />

    <ImageView
        android:id="@+id/bgBtn"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/right"
        android:background="@drawable/bg_galileo2"
        app:tint="#B3000000"
        android:padding="6dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintHorizontal_bias="0.57"
        />
</androidx.constraintlayout.widget.ConstraintLayout>
