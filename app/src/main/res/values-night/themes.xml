<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FizikaniOrganamiz" parent="Theme.Material3.DayNight.NoActionBar">

    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textColor">@color/search_text</item> <!-- Matn har doim qora -->
        <item name="android:textColorHint">@color/search_hint</item> <!-- Hint rangi -->
        <item name="android:queryBackground">@color/search_background</item> <!-- Fondagi rang -->
        <item name="android:tint">@color/search_icon</item> <!-- Qidiruv ikon rangi -->
    </style>

</resources>
